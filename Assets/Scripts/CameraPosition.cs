using UnityEngine;
using static AllLevels;

public class CameraPosition : MonoBehaviour {

    public void Update() {
        // na osnovu levela nekako proracuna x,y,z
        //Debug.Log("Postavio");
        //Camera.main.transform.position = new Vector3Int(6, 8, -12);
    }
}
